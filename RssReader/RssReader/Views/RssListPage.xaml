<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RssReader.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModels="using:RssReader.ViewModels"
    x:Class="RssReader.Views.RssListPage"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel.DataContext>
            <ViewModels:RssListPageViewModel/>
        </StackPanel.DataContext>
        <TextBlock Text="Ynet - חדשות" FontSize="15" Height="26" Margin="10,0,0,0"/>
        <ListBox  ItemsSource="{Binding Path=List ,Mode=TwoWay}" SelectedItem="{Binding CurrentItemSelected, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Title}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Page>
